spring:
  datasource:
    url: jdbc:h2:mem:test
    username: user
    password: pwd
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false
